@model PasswordRecoveryModel
@{
    Layout = "_ColumnsOnes";

    //title
    Html.AddTitleParts(T("PageTitle.PasswordRecovery").Text);
    //page class
    Html.AppendPageCssClassParts("html-password-recovery-page");
}
<div class="page password-recovery-page">
    
    <div class="page-body">

        <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
            <div class="account-login">

                <form asp-route="PasswordRecovery" method="post" class="form-my-account">
                    <div asp-validation-summary="ModelOnly" class="message-error"></div>
                    <p class="tooltip">@T("Account.PasswordRecovery.Tooltip")</p>
                    @if (!string.IsNullOrEmpty(Model.Result))
                    {
                        <div class="result">
                            @Model.Result
                        </div>
                    }
                    <h2 class="title"><i class="fa fa-user"></i> E-Posta Adresiniz</h2>
                    <div class="fieldset">
                        <div class="form-fields">
                            <p class="inputs">
                                <input asp-for="Email" class="email" />
                                <nop-required />
                                <span asp-validation-for="Email"></span><span class="icon-mail"></span>
                            </p>
                        </div>
                    </div>
                    <div class="buttons">
                        <input type="submit" name="send-email" class="button-1 password-recovery-button" value="@T("Account.PasswordRecovery.RecoverButton")" />
                    </div>
                </form>

            </div>

        </div>

    </div>
</div>